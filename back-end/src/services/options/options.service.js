const { selections, suggestions } = require('../../config/options');

const upsertOption = async (body) => {
  return;
};

const getOptions = async () => {
  return { selections, suggestions };
};

module.exports = {
  upsertOption,
  getOptions,
};
